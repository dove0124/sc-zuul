server:
  port: 8091
  
spring:
  cloud:
    config:
      label: master
      #      uri: http://localhost:9090
      discovery:
        enabled: true
        service-id: sc-config-server
      name: config-info, app-config, sluth-config
      profile: dev
  application:
    name: sc-data-service
